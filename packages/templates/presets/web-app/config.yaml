version: 0.1.0

tdd:
  enabled: true
  coverage_threshold: 80

constitution:
  enabled: true

knowledge:
  enabled: true

compression:
  enabled: true
  engine: builtin
  builtin:
    json_max_keys: 30
    code_max_lines: 80
    prose_max_sentences: 8
