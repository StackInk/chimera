version: 0.1.0

tdd:
  enabled: false
  coverage_threshold: 80

constitution:
  enabled: false

knowledge:
  enabled: false
  providers:
    codegraph:
      enabled: false
      # binary: codegraph
      # max_results: 5
      # timeout_ms: 10000

compression:
  enabled: true
  engine: builtin
  builtin:
    json_max_keys: 20
    code_max_lines: 50
    prose_max_sentences: 5
