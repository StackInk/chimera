version: 0.1.0

tdd:
  enabled: false
  coverage_threshold: 80

constitution:
  enabled: false

knowledge:
  enabled: false

compression:
  enabled: true
  engine: builtin
  builtin:
    json_max_keys: 20
    code_max_lines: 50
    prose_max_sentences: 5
